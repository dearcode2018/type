/**
 * StringServiceImpl.java
 * @author  qye.zheng
 * 	version 1.0
 */
package com.hua.service.impl;

import com.hua.service.StringService;

/**
 * StringServiceImpl
 * 描述: 
 * @author  qye.zheng
 */
public final class StringServiceImpl extends CoreServiceImpl implements
		StringService
{

	/**
	 字符串
	 类声明: 
	 final class java.lang.String
	 
	 String 对象是可以不可变对象，底层
	 由字符数组构成，数组元素不能改变，
	 其不提供修改自身的方法，所有的修改
     返回的结果都是新的字符串对象，原来的
	 字符串保持不变.
	 
	 String对象作为方法的参数，这个属于引用传递，
	 只是将实参指向的字符串的地址传到形参，形参
	 在方法中实施的变动，将返回一个新的字符串对象，
	 若形参接受该返回结果，则指向新的引用对象，
	 而并不影响实参的指向.
	 
	 
	 
	 
	 
	 
	 
	 
	 */
	
}
